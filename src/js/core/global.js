const sel = {
  $document: $(document),
  $window: $(window),
  $body: $(document.body),
  $htmlBody: $('html,body'),
  $html: $('html'),
};

const _library = {
  test: 'test',
};

export { sel, _library };
